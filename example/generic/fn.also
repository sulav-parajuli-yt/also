// A function that adds 2 numbers
function sum(m, n) {
    var t = m + n
    return t
}

// A function that calculates sum, difference
function sumOrDiff(a,b,what) {
    if(what == 1) {
        return a + b
    } else {
        return a - b
    }
}

// Function returning multiple thing, first return should end the function
function hehe(a,b,what) {
        return a + b
        return a * b
        return a * b
        return a - b
}

var xz = hehe(15, 9, 1)
print xz