// Define some variables
var x = 5;
var y = (3 + 2);
var z = x * y;

// Perform some assignments
x = z + 10;
y = (x - 3) * 2;
z = y - x;

// Using expressions
var result = (x + y) * (z - 10);

// Final assignment
finalResult = result + x;